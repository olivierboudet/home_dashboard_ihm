import Home from 'components/Home/home';
import SolarHistory from 'components/SolarHistory/solarHistory';

const routes = [
  {
    path: '/',
    component: Home
  },
  {
    path: '/solar',
    component: SolarHistory
  }
];

export default routes;
